{
  "version": "1.0.0",
  "name": "synergylearn-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "lint": "turbo lint",
    "dev:api": "pnpm --filter api dev",
    "dev:portal": "pnpm --filter portal dev",
    "dev:studio": "pnpm --filter studio dev",
    "dev:admin": "pnpm --filter admin dev"
  },
  "devDependencies": {
    "prettier": "latest",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@8.6.10",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}